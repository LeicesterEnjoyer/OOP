package oop.dataformat;

/**
 * DateFormats enum defines different date format options.
 * Each enum value represents a specific date format, such as Bulgarian (BG), French (FR), or including month names in words (MONTH_WITH_WORD).
 */
public enum DateFormats { BG, FR, MONTH_WITH_WORD }