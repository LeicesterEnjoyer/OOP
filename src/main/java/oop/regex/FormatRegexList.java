package oop.regex;

/**
 * FormatRegexList enum defines different types of regular expression patterns.
 * Each enum value represents a specific pattern type, such as personal names, email addresses, or white spaces.
 */
public enum FormatRegexList { PERSONAL_NAME, EMAIL, WHITE_SPACES }